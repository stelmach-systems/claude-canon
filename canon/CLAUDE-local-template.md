# Local: this device's layers and tools

<!-- TODO: fill in for your device/organization, or delete lines that don't apply.
     This part is NOT updated by install.sh — it's yours to own. -->

- Configuration layers (don't duplicate across them): the organization's operational rules → <path to the org's playbook, if one exists>; file/code workflow → <e.g. ~/Developer/CLAUDE.md>; domain context → the project's CLAUDE.md; ephemeral facts and learnings → the project's auto-memory (Claude Code: `~/.claude/projects/<project>/memory/`).
- Recall when returning to a topic: <e.g. your org's recall skill>; change on live production: <e.g. a dedicated production-change skill>.
- Full orchestration patterns (extends the canon's essence): `knowledge/orchestration-patterns.md` in your clone of the claude-canon repo.
- This device's stakes calibration (canon rule: no threshold = fail-closed): money-significance threshold: <TODO, e.g. $150 — below it "significant", at/above it "high">; inventory of LIVE PRODUCTION: <TODO: list — shop/URL, VPS, automations writing to live systems>; standing consents (what not to ask about): <TODO, e.g. commits in working repos>.
- Canon rule governance (incident/seed intake, status, exit, evidence ledger, size limits): see the claude-canon repo README. Local rules (this section) should be committed in your local `~/.claude` repo with references to the incidents/sessions that motivated them.
